DATABASE_URL="postgresql://studiodesk:studiodesk@localhost:5432/studiodesk?schema=public"
SESSION_COOKIE=studiodesk_session
WEB_ORIGIN=http://localhost:3000
API_PORT=4000
# Optional: leave blank to use the local "dev upgrade" path (no Stripe account needed).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
# Optional Google Calendar. App still runs without these.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/calendar/callback
